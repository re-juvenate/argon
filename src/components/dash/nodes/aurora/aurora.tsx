import type { CSSProperties } from "react";
import Node from "../../Node";

const Aurora = ({ style }: { style?: CSSProperties }) => {
  return <Node color="#3f4fd3" name="Aurora RDS" style={style} />;
};

export default Aurora;
