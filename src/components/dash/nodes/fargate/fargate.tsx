import type { CSSProperties } from "react";
import Node from "../../Node";

const Fargate = ({ style }: { style?: CSSProperties }) => {
  return <Node color="#d86613" name="Fargate" style={style} />;
};

export default Fargate;
